#!/usr/bin/env python3
"""

"""


def poly_derivative(poly):
    if poly==0:
        return None
    else:
        n = len(polly)-1
        der[n]
        i = 1
        for i<=n:
            der[i] = polly[i]*i
            i += 1
        return der
